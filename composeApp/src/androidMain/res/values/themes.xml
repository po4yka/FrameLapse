<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.FrameLapse" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>

    <!-- Base Splash Theme for pre-API 31 (no splash screen API) -->
    <style name="Theme.FrameLapse.Splash" parent="Theme.FrameLapse">
        <!-- Uses standard theme on older devices -->
    </style>
</resources>
